# Employee-Manager
A Database System subject mini project.

# Programming Language
We are using Python with Flask for back-end, and MySQL (Laragon) for database system.

# Flask Installation
```bash
pip install Flask
pip install flask-mysqldb
```
